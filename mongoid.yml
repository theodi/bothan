production:
  sessions:
    default:
      database: metrics_api_production
      hosts:
        - localhost
development:
  sessions:
    default:
      database: metrics_api_development
      hosts:
        - localhost
test:
  sessions:
    default:
      database: metrics_api_test
      hosts:
        - localhost